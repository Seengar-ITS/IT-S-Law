import React,{useEffect,useState}from'react';import{supabase}from'../lib/supabase.js';import{requireAuth}from'../lib/auth.js';import*as S from'../styles.js';
export default function Agreement(){const id=window.location.pathname.split('/')[2];const[ag,setAg]=useState(null);const[parties,setParties]=useState([]);useEffect(()=>{requireAuth(window.location.href);supabase.from('agreements').select('*').eq('id',id).single().then(({data})=>setAg(data));supabase.from('agreement_parties').select('*').eq('agreement_id',id).then(({data})=>setParties(data||[]));  },[id]);
if(!ag)return React.createElement('div',{style:S.page},React.createElement('p',{style:S.muted},'Loading...'));
return React.createElement('div',{style:S.page},React.createElement('div',{style:{display:'flex',justifyContent:'space-between',marginBottom:'1.5rem'}},React.createElement('h1',{style:{...S.h1,marginBottom:0}},ag.title),React.createElement('span',{style:S.badge('#7c3aed')},ag.status)),React.createElement('div',{style:S.card},React.createElement('h2',{style:S.h2},'Terms'),React.createElement('p',{style:{...S.muted,whiteSpace:'pre-wrap',lineHeight:1.8}},ag.terms)),React.createElement('div',{style:S.card},React.createElement('h2',{style:S.h2},'Parties'),parties.map(p=>React.createElement('div',{key:p.id,style:{display:'flex',justifyContent:'space-between',marginBottom:'0.5rem'}},React.createElement('span',{style:S.muted},'Party '+parties.indexOf(p)+1),React.createElement('span',{style:{color:p.signed?'#22c55e':'#f59e0b'}},p.signed?'✓ Signed':'Pending')))),React.createElement('button',{style:S.btn,onClick:()=>window.location.href='/sign/'+id},'Invite to Sign'));}